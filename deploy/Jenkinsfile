pipeline {
  agent any
  stages {
    stage('Run Ansible Playbook') {
      steps {
        sh '''
KEY=credentials(\'Ansible-target-key\')

ansible-playbook -i inventory/dev --private-key playbooks/site.yml'''
      }
    }

  }
}