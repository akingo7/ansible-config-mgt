pipeline {
  agent any
  stages {
    stage('Run Ansible Playbook') {
      steps {
        ansiblePlaybook(playbook: 'playbooks/site.yml', disableHostKeyChecking: true, vaultCredentialsId: '	Hlove', inventory: 'inventory/dev')
      }
    }

  }
}