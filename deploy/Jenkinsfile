pipeline {
  agent any
  stages {
    stage('Run Ansible Playbook On Dev') {
      steps {
        ansiblePlaybook(playbook: '../playbook/site.yml', become: true, becomeUser: 'root', inventory: '../inventory/dev')
      }
    }

  }
}